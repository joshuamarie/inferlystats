% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/define_model_process.R
\name{process-model-id}
\alias{process-model-id}
\alias{process_model_id}
\alias{process_model_id.formula}
\alias{process_model_id.rel}
\alias{process_model_id.compare_by}
\alias{process_model_id.cont_tab}
\alias{process_model_id.prop_model}
\alias{process_model_id.pairwise}
\alias{process_model_id.selected_vars}
\alias{process_model_id.one_sample}
\alias{process_model_id.infer_model}
\title{Validate and extract data for a model ID}
\usage{
process_model_id(model_id, data)

\method{process_model_id}{formula}(model_id, data)

\method{process_model_id}{rel}(model_id, data)

\method{process_model_id}{compare_by}(model_id, data)

\method{process_model_id}{cont_tab}(model_id, data)

\method{process_model_id}{prop_model}(model_id, data)

\method{process_model_id}{pairwise}(model_id, data)

\method{process_model_id}{selected_vars}(model_id, data)

\method{process_model_id}{one_sample}(model_id, data)

\method{process_model_id}{infer_model}(model_id, data)
}
\arguments{
\item{model_id}{A model ID object from \code{\link[=rel]{rel()}}, \code{\link[=cont_tab]{cont_tab()}},
\code{\link[=prop_model]{prop_model()}}, \code{\link[=pairwise]{pairwise()}}, \code{\link[=selected_vars]{selected_vars()}}, or \code{\link[=one_sample]{one_sample()}}.}

\item{data}{A data frame against which variables in \code{model_id} are
resolved.}
}
\value{
A named list containing:
\itemize{
\item \verb{$data} — a subsetted data frame containing only the relevant columns,
or a contingency table for \code{cont_tab()}
\item \verb{$vars} — a named character vector or list of resolved column names
\item \verb{$mat} — a contingency table, only present for \code{cont_tab()}
\item \verb{$pairs} — a list of variable pairs, only present for \code{pairwise()}
\item \verb{$formula} — the formula, only present for formula models
}
}
\description{
\code{process_model_id()} resolves the variables declared in a model ID object
against a data frame, validates that all referenced columns exist, and
returns a named list of processed data ready for use by test
implementations.
}
\details{
\code{process_model_id_global()} is the counterpart for the no-data path — when
variables are resolved from the calling environment rather than a data
frame.
}
\section{Error behaviour}{

Errors immediately if any referenced column is missing from \code{data}, with
an informative message listing both the missing columns and the available
ones.
}

\examples{
m = rel(group, extra)
process_model_id(m, sleep)

m = cont_tab(vs, am)
process_model_id(m, mtcars)

}
\seealso{
\code{\link[=define_model]{define_model()}}, \code{\link[=rel]{rel()}}, \code{\link[=cont_tab]{cont_tab()}}, \code{\link[=prop_model]{prop_model()}},
\code{\link[=pairwise]{pairwise()}}, \code{\link[=selected_vars]{selected_vars()}}, \code{\link[=one_sample]{one_sample()}}
}
