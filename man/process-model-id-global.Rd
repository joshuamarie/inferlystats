% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/define_model_process.R
\name{process-model-id-global}
\alias{process-model-id-global}
\alias{process_model_id_global}
\alias{process_model_id_global.rel}
\alias{process_model_id_global.formula}
\alias{process_model_id_global.cont_tab}
\alias{process_model_id_global.infer_model}
\title{Validate and extract data for a model ID from the calling environment}
\usage{
process_model_id_global(model_id)

\method{process_model_id_global}{rel}(model_id)

\method{process_model_id_global}{formula}(model_id)

\method{process_model_id_global}{cont_tab}(model_id)

\method{process_model_id_global}{infer_model}(model_id)
}
\arguments{
\item{model_id}{A model ID object from \code{\link[=rel]{rel()}}, \code{\link[=cont_tab]{cont_tab()}},
\code{\link[=prop_model]{prop_model()}}, \code{\link[=pairwise]{pairwise()}}, \code{\link[=selected_vars]{selected_vars()}}, or \code{\link[=one_sample]{one_sample()}}.}
}
\value{
A named list with the same structure as \code{\link[=process_model_id]{process_model_id()}}:
\itemize{
\item \verb{$data} — a data frame or contingency table constructed from the
evaluated variables
\item \verb{$vars} — a named character vector or list of resolved variable names
\item \verb{$mat} — a contingency table, only present for \code{cont_tab()}
\item \verb{$formula} — the formula, only present for formula models
}
}
\description{
\code{process_model_id_global()} is the counterpart to \code{\link[=process_model_id]{process_model_id()}} for
the no-data path. Instead of resolving variables against a data frame, it
evaluates them from the calling environment via lexical scoping.
}
\details{
This is used when \code{\link[=define_model]{define_model()}} is called without a data frame — for
instance when passing raw vectors or pre-summarized values directly as
model ID arguments.
}
\section{Error behaviour}{

Errors if the model type is not supported on the global path, with an
informative message suggesting the user supply a data frame via
\code{\link[=define_model]{define_model()}}.
}

\examples{
m = rel(sleep$group, sleep$extra)
process_model_id_global(m)

}
\seealso{
\code{\link[=process_model_id]{process_model_id()}}, \code{\link[=define_model]{define_model()}}
}
