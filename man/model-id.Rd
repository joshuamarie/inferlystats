% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/define_model.R
\name{model-id}
\alias{model-id}
\alias{rel}
\alias{compare_by}
\alias{cont_tab}
\alias{prop_model}
\alias{pairwise}
\alias{selected_vars}
\alias{one_sample}
\title{Model ID constructors for use inside \code{define_model()}}
\usage{
rel(x, resp, ...)

compare_by(x, grp, ...)

cont_tab(ind, dep, ...)

prop_model(x, ...)

pairwise(..., .dir = "leq")

selected_vars(...)

one_sample(var, ...)
}
\arguments{
\item{x}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> The variable whose
proportion is being modeled. For the global path, a count of successes.}

\item{resp}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> The response
variable. For \code{rel()}, this is the outcome being measured across groups.}

\item{...}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> For \code{pairwise()}
and \code{selected_vars()}: two or more variables to include.}

\item{ind}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> The independent
(row) variable in the contingency table.}

\item{dep}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> The dependent
(column) variable in the contingency table.}

\item{.dir}{Direction of pairing for \code{pairwise()}. One of \code{"leq"}
(default), \code{"lt"}, \code{"gt"}, \code{"geq"}, \code{"eq"}, \code{"neq"}, or \code{"all"}.}

\item{var}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> The single variable
to analyze.}

\item{size}{For \code{prop_model()} on the global path only: the total number
of trials.}
}
\value{
An \code{infer_model} object of the corresponding subclass —
\code{rel}, \code{cont_tab}, \code{prop_model}, \code{pairwise}, \code{selected_vars}, or
\code{one_sample}.
}
\description{
These functions declare the structure of the analysis — which variables to
use and how they relate to each other. They are always passed as the
\code{to_analyze} argument to \code{\link[=define_model]{define_model()}}, or called standalone for the
global (no data frame) path.
}
\details{
Each constructor produces an \code{infer_model} object carrying the variable
declarations as unevaluated expressions. Variables are resolved lazily —
either against a data frame inside \code{\link[=define_model]{define_model()}}, or from the calling
environment when no data frame is supplied.
}
\section{\code{rel()}}{

Declares a two-variable relationship between a grouping variable \code{x} and a
response variable \code{resp}. Used for tests that compare a response across
groups, such as and \code{COR_TEST()}.
}

\section{\code{cont_tab()}}{

Declares a contingency table from two categorical variables \code{ind} and
\code{dep}. Used for tests of independence or association, such as
\code{\link[=CHI2_TEST]{CHI2_TEST()}}.
}

\section{\code{prop_model()}}{

Declares a proportion model from a binary variable or raw success/trial
counts. Used for \code{BINOM_TEST()} and \code{PROP_TEST()}.
}

\section{\code{pairwise()}}{

Declares all pairwise combinations of two or more variables. The direction
of pairing is controlled by \code{.dir}. Used for pairwise \code{\link[=TTEST]{TTEST()}} and similar
tests.
}

\section{\code{selected_vars()}}{

Declares an arbitrary set of variables with no assumed structure. Used when
the test itself determines how the variables are used.
}

\section{\code{one_sample()}}{

Declares a single variable for one-sample inference — testing or estimating
a population parameter from a single group.
}

\examples{
# rel() — grouping variable x response
sleep |> define_model(rel(group, extra))
define_model(rel(sleep$group, sleep$extra))

# cont_tab() — contingency table
mtcars |> define_model(cont_tab(vs, am))

# prop_model() — proportion from data or counts
mtcars |> define_model(prop_model(vs))
define_model(prop_model(x = 10, size = 19))

# pairwise() — all pairwise combinations
sleep |> define_model(pairwise(extra, group))

# selected_vars() — arbitrary variable set
mtcars |> define_model(selected_vars(mpg, wt, hp))

# one_sample() — single variable
sleep |> define_model(one_sample(extra))

}
\seealso{
\code{\link[=define_model]{define_model()}}, \code{\link[=run_test]{run_test()}}, \code{\link[=prepare_test]{prepare_test()}}
}
